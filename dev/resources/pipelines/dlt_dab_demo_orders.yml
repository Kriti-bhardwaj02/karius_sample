resources:
  pipelines:
    pipeline_dlt_demo_dab:
      name: dlt_demo_dab
      clusters:
        - label: default
          node_type_id: m5d.large
          num_workers: 1
      libraries:
        - notebook:
            path: ../notebooks/dlt_dab_demo_orders.sql
      schema: ${resources.schemas.bronze_schema.name}
      development: true
      catalog: ${resources.schemas.bronze_schema.catalog_name}